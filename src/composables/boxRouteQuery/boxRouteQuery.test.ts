import { describe, it, expect } from 'vitest'
import { useBoxRouteQuery } from './boxRouteQuery'

describe('useBoxRouteQuery composable', () => {
  it('should be defined', () => {
    expect(useBoxRouteQuery).toBeDefined()
  })
})
